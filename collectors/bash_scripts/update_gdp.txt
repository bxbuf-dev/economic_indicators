# Стандартное обновление (проверка + загрузка)
./update_gdp.sh

# Только проверка ревизий без обновления
./update_gdp.sh --check-only

# Принудительная полная загрузка истории
./update_gdp.sh --force-historical